Example grammar for a configuration file:

configuration: '[' \w bindings \w ']'

bindings: binding | binding \w ',' \w bindings

binding: name \w '=' \w value

name: [a-zA-Z][0-9a-zA-Z]*

value: int | bool

int: [0-9] | [1-9][0-9]*

bool: "true" | "false"

\w: [ \t\n]*
